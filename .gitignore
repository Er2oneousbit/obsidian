# --- Claude Code / internal working files (not part of the published vault) ---
CLAUDE.md
TODO.md
.claude/
.obsidian/

# --- Raw personal HTB Academy course notes — excluded from the public release ---
# These are rough personal notes taken while working through paid Hack The Box
# Academy material. The polished, independently-rewritten reference versions live
# in "CPTS v2 (claude)/" and "CWES Claude/" and ARE published.
/Class notes/HTB Academy/CWES/
/Class notes/HTB Academy/CPTS Path/
/Class notes/HTB Academy/AI Red Teaming/
/Class notes/HTB Academy/Boxes/

# --- Burp Suite project files — multi-hundred-MB binaries, not vault content ---
# Keep the notes/scripts that reference them (burp_parse.py, *.md), not the dumps.
*.burp
*.burp.backup
*.burp.bak
*.burpstate
burp_history.txt

# --- Python bytecode caches from exploit scripts ---
__pycache__/
*.pyc

# --- Tool session/scratch state (regenerated on every run) ---
*.restore

# --- Box loot: local-only. The exploit scripts and .md writeups are the record;
# the raw haul (dumps, keys, backups, DB files) stays off the remote.
loot/
